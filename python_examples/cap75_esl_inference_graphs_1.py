# Conceito de passagem de mensagem entre no i e j:
# m_ij(xj) = sum_xi (psi(xi, xj) * phi(xi) * prod_k(m_ki(xi)))
# Onde phi sao os potenciais locais e psi as arestas.
# Esta e a base para algoritmos como o Viterbi em modelos ocultos.